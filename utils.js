// Fonctions pures : formater, filtrer, trier (testées)

// Texte résultats
export const texteCompteur = (combienAffiches, total) => {
  return `${combienAffiches} jardins trouvés • ${total} résultats`;
};

// Formatage
// export const formaterSurface = (surface) => `${surface} m²`;

// Création de carte